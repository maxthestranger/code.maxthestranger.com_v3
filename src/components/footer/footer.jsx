import * as React from "react";
import { footer } from "./footer.module.scss";

const Footer = () => {
  return (
    <footer className={footer}>
      <small>Designed & Built with ❤︎ by Max The Stranger</small>
    </footer>
  );
};

export default Footer;
